---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useSpellCheck.ts
  - src/components/note/index.tsx
  - src/lib/local-spell-checker.ts
autonomous: true
requirements: [SPELLCHECK-BUGFIX]

must_haves:
  truths:
    - "Spelling underlines do not flash/disappear when re-checking"
    - "Switching notes clears previous note spelling errors"
    - "Ignore action calls learnWord exactly once, not twice"
    - "Length guard is consistent between scheduleSpellCheck and checkSpelling"
  artifacts:
    - path: "src/hooks/useSpellCheck.ts"
      provides: "Flash fix, redundant ignore fix, length guard fix"
    - path: "src/components/note/index.tsx"
      provides: "clearSpellCheck on note switch"
    - path: "src/lib/local-spell-checker.ts"
      provides: "cleanWord regex fix, dead code removal"
  key_links:
    - from: "src/components/note/index.tsx"
      to: "useSpellCheck.clearSpellCheck"
      via: "called inside selectNote wrapper or useEffect on selectedNoteIndex"
      pattern: "clearSpellCheck"
---

<objective>
Fix 5 spell-check bugs: flash on re-check, stale errors on note switch, redundant ignore calls, length guard inconsistency, and dead code in local-spell-checker.

Purpose: Eliminate visual glitches and code inconsistencies in the spell-check system.
Output: Clean, bug-free spell-check behavior across all three files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/hooks/useSpellCheck.ts
@src/components/note/index.tsx
@src/lib/local-spell-checker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix useSpellCheck bugs (flash, redundant ignore, length guard)</name>
  <files>src/hooks/useSpellCheck.ts</files>
  <action>
Three fixes in useSpellCheck.ts:

1. **Flash on re-check (line 110):** Remove `setSpellingErrors([])` from `checkSpelling`. The idle callback already sets errors when results arrive. Removing the eager clear prevents the flash where underlines disappear then reappear. The `invalidateErrors` in `scheduleSpellCheck` already handles removing stale errors at the edit position.

2. **Redundant ignore (lines 226-227):** In `handleIgnoreSpelling`, remove the `adaptiveDictionary.learnWord(error.word)` call. Keep only `addToCustomDictionary(error.word)` since `addToCustomDictionary` already calls `adaptiveDictionary.learnWord` internally. This prevents double-learning.

3. **Length guard inconsistency (line 99 vs 153):** In `checkSpelling` (line 99), change `text.trim().length < 3` to `text.trim().length <= 3` so it matches `scheduleSpellCheck`'s `> 3` guard. Both should agree: text must have more than 3 trimmed characters to trigger spell-check. Alternatively, change both to use `< 3` / `>= 3` — the key is consistency. Use `< 3` in checkSpelling and `>= 3` in scheduleSpellCheck (i.e., minimum 3 chars).

  </action>
  <verify>
    <automated>cd /Users/tareq/codes/Bangla/notebook && grep -n "setSpellingErrors\(\[\]\)" src/hooks/useSpellCheck.ts | grep -v "// Clear" | head -5 && grep -c "adaptiveDictionary.learnWord" src/hooks/useSpellCheck.ts && grep -n "length.*3" src/hooks/useSpellCheck.ts</automated>
  </verify>
  <done>
- checkSpelling no longer calls setSpellingErrors([]) before idle callback
- handleIgnoreSpelling calls addToCustomDictionary only (not both learnWord and addToCustomDictionary)
- Length guards are consistent: both use the same threshold (minimum 3 chars)
  </done>
</task>

<task type="auto">
  <name>Task 2: Clear spell errors on note switch</name>
  <files>src/components/note/index.tsx</files>
  <action>
In NoteComponent (index.tsx):

1. Destructure `clearSpellCheck` from `useSpellCheck` (it's currently not destructured — line 47-55).

2. Add a `useEffect` that watches `selectedNoteIndex` and calls `clearSpellCheck()` when it changes. Also reset `prevTextRef` state by scheduling a fresh spell check after a short delay if the new note has content and is in Bangla mode:

```typescript
useEffect(() => {
  clearSpellCheck();
}, [selectedNoteIndex, clearSpellCheck]);
```

This ensures when user clicks a different note in the sidebar, old spelling underlines from the previous note are immediately removed rather than persisting until the next spell-check cycle.
  </action>
  <verify>
    <automated>cd /Users/tareq/codes/Bangla/notebook && grep -n "clearSpellCheck" src/components/note/index.tsx</automated>
  </verify>
  <done>
- clearSpellCheck is destructured from useSpellCheck
- useEffect calls clearSpellCheck when selectedNoteIndex changes
- Switching notes immediately clears spelling underlines
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean up dead code in local-spell-checker</name>
  <files>src/lib/local-spell-checker.ts</files>
  <action>
Three cleanups in local-spell-checker.ts:

1. **cleanWord regex (line 292):** Add the `g` flag to the regex: `word.replace(/[...]/g, '')` so it removes ALL punctuation occurrences, not just the first. Current regex `/[...]/` without `g` only strips the first match.

2. **Remove isBanglaWord function (lines 284-287):** This function is never called anywhere in the codebase. Delete it. The spell-checker's word regex `[\u0980-\u09FF]+` already ensures only Bangla words are checked.

3. **Remove validBanglaCharCombinations export (lines 38-50, 379):** The array is unused — it was marked as dead code with a comment "Keep for type compatibility — unused but referenced elsewhere" but grep confirms nothing imports it. Remove the array and its export statement.
  </action>
  <verify>
    <automated>cd /Users/tareq/codes/Bangla/notebook && grep -n "isBanglaWord\|validBanglaChar" src/lib/local-spell-checker.ts && grep -rn "validBanglaCharCombinations\|isBanglaWord" src/ --include="*.ts" --include="*.tsx" | grep -v "local-spell-checker"</automated>
  </verify>
  <done>
- cleanWord uses /g flag for global replacement
- isBanglaWord function removed (no callers)
- validBanglaCharCombinations array and export removed (no importers)
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. `pnpm lint` passes with no new errors
2. `pnpm build` compiles without type errors (do NOT run during dev per CLAUDE.md — verify with lint only)
3. Manual test: open app, type Bangla text, verify underlines appear without flashing, switch notes and confirm old errors clear
</verification>

<success_criteria>
- No flash/flicker when spell-check re-runs (underlines stay stable during re-check)
- Note switch clears previous spelling errors immediately
- handleIgnoreSpelling calls learnWord exactly once (through addToCustomDictionary)
- Length guard consistent between scheduleSpellCheck and checkSpelling
- No dead code (isBanglaWord, validBanglaCharCombinations removed, cleanWord regex fixed)
</success_criteria>

<output>
After completion, create `.planning/quick/260328-26o-investigate-and-fix-spell-check-function/260328-26o-SUMMARY.md`
</output>
