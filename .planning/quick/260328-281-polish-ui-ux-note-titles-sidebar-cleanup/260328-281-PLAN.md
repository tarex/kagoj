---
phase: quick
plan: 260328-281
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/note/use-notes.ts
  - src/components/note/note-list.tsx
  - src/components/note/index.tsx
  - src/components/note/toolbar.tsx
  - src/components/note/keyboard-shortcuts-panel.tsx
  - src/app/globals.css
autonomous: true
requirements: [TITLE-FIELD, SIDEBAR-TITLES-ONLY, KEYBOARD-SHORTCUT-MAPPER, UI-POLISH]

must_haves:
  truths:
    - "Each note has a dedicated title field above the content editor"
    - "Sidebar shows only note titles (not content previews)"
    - "Topbar has a keyboard shortcut reference button that opens a panel listing all shortcuts"
    - "Title persists across save/load cycles in localStorage"
  artifacts:
    - path: "src/components/note/use-notes.ts"
      provides: "Note interface with title field, title getter/setter"
      contains: "title: string"
    - path: "src/components/note/note-list.tsx"
      provides: "Title-only sidebar items"
    - path: "src/components/note/keyboard-shortcuts-panel.tsx"
      provides: "Keyboard shortcut reference overlay"
      exports: ["KeyboardShortcutsPanel"]
    - path: "src/components/note/index.tsx"
      provides: "Title input field above editor, shortcut panel toggle"
    - path: "src/app/globals.css"
      provides: "Styles for title input and shortcut panel"
  key_links:
    - from: "src/components/note/index.tsx"
      to: "src/components/note/use-notes.ts"
      via: "title state from useNotes hook"
      pattern: "currentTitle|setCurrentTitle"
    - from: "src/components/note/note-list.tsx"
      to: "Note interface"
      via: "note.title display"
      pattern: "note\\.title"
---

<objective>
Add note title field, clean up sidebar to show titles only, and add keyboard shortcut reference panel to topbar.

Purpose: Transform the note-taking experience from content-preview-based navigation to clean title-based organization, and make shortcuts discoverable.
Output: Updated note data model with title, cleaner sidebar, title input above editor, shortcut panel component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/note/use-notes.ts
@src/components/note/note-list.tsx
@src/components/note/index.tsx
@src/components/note/toolbar.tsx
@src/app/globals.css
@CLAUDE.md

<interfaces>
<!-- Current Note interface (use-notes.ts) -->
```typescript
interface Note {
  content: string;
  date: string;
}
```

<!-- Current useNotes return -->
```typescript
{
  notes: Note[];
  currentNote: string;
  selectedNoteIndex: number | null;
  setCurrentNote: (content: string) => void;
  createNewNote: () => void;
  selectNote: (index: number) => void;
  deleteNote: (index: number) => void;
  saveNotes: (notes: Note[]) => void;
  saveCurrentNote: () => void;
}
```

<!-- Toolbar props -->
```typescript
interface ToolbarProps {
  onFormatBold: () => void;
  onFormatItalic: () => void;
  onFormatUnderline: () => void;
  onFormatStrikethrough: () => void;
  onFormatCode: () => void;
  onFormatHighlight: () => void;
  onInsertBullet: () => void;
  onInsertNumberedList: () => void;
  onCheckSpelling: () => void;
  isCheckingSpelling: boolean;
  isBanglaMode: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add title field to Note model, update useNotes hook, update sidebar and editor</name>
  <files>src/components/note/use-notes.ts, src/components/note/note-list.tsx, src/components/note/index.tsx, src/app/globals.css</files>
  <action>
**1. Update Note interface and useNotes hook** (`use-notes.ts`):
- Add `title: string` to `Note` interface
- Add `currentTitle` state (string, default empty)
- Add `setCurrentTitle` to the returned API — works like `setCurrentNote` (updates title in notes array when editing existing note, throttle-saves to localStorage)
- In `createNewNote`: include `title: currentTitle || ''` when creating the Note object, clear title after save
- In `selectNote`: set `currentTitle` from `notes[index].title`
- In `updateCurrentNote` (the internal function behind `setCurrentNote`): also update the note's content in the array (already does this)
- Add `updateCurrentTitle` callback that mirrors `updateCurrentNote` pattern: sets `currentTitle`, updates `notes[selectedNoteIndex].title`, throttle-saves
- Migration: when loading from localStorage, if a note lacks `title`, default to empty string (backward compat)
- Return `currentTitle` and `setCurrentTitle: updateCurrentTitle` from the hook

**2. Update NoteList** (`note-list.tsx`):
- Update local `Note` interface to include `title: string`
- Change display logic: show `note.title` as the primary text (bold, 14px, single line truncated). If title is empty, show "শিরোনামহীন" (Untitled in Bangla) in italic muted style
- Remove the `.note-preview` content preview entirely — sidebar items show only title + date
- Keep date display as-is (bn-BD locale)
- Keep delete button as-is

**3. Update NoteComponent** (`index.tsx`):
- Destructure `currentTitle` and `setCurrentTitle` from `useNotes()`
- Add a title input (`<input>`) above the NoteEditor inside `.editor-main`, before `.editor-wrapper`
- The title input: `type="text"`, `placeholder="শিরোনাম..."` (Title... in Bangla), class `note-title-input`, value bound to `currentTitle`, onChange calls `setCurrentTitle`
- The title input should NOT go through BanglaInputHandler — it is a plain `<input>` that receives Bangla text naturally when the OS IME or Avro is active (matching how the textarea works when `isBanglaMode` is false). However, if the user wants phonetic typing in the title, add `onKeyDown` handler that calls `banglaInputHandler.processInputKeyPress` with a ref to the title input when `isBanglaMode` is true. Create a `titleInputRef` using `useRef<HTMLInputElement>(null!)`.
- Wire the title input's onKeyDown to process Bangla input same as the textarea (call `banglaInputHandler.processInputKeyPress(titleInputRef, currentTitle, setCurrentTitle, e)`) when `isBanglaMode` is true

**4. Add CSS styles** (`globals.css`):
- `.note-title-input`: full width, no border, transparent background, font-family var(--font-body), font-size 24px, font-weight 600, color var(--text-primary), padding 48px 56px 0 56px (match editor left/right padding but no bottom padding), outline none, line-height 1.4, caret-color var(--accent-primary). placeholder color var(--text-muted), placeholder font-weight 300, placeholder font-style italic
- `.note-title-label` in sidebar: font-size 14px, font-weight 600, color var(--text-primary), single line truncation (overflow hidden, text-overflow ellipsis, white-space nowrap), line-height 1.4
- `.note-title-label.untitled`: font-style italic, color var(--text-muted), font-weight 400
- Update responsive media queries: at max-width 900px, title input padding 32px 36px 0 36px; at max-width 640px, padding 24px 20px 0 20px
  </action>
  <verify>
    <automated>cd /Users/tareq/codes/Bangla/notebook && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Notes have title field persisted in localStorage. Sidebar shows titles only. Title input renders above editor with Bangla input support. Existing notes without title load gracefully with empty title.</done>
</task>

<task type="auto">
  <name>Task 2: Add keyboard shortcuts panel to topbar</name>
  <files>src/components/note/keyboard-shortcuts-panel.tsx, src/components/note/index.tsx, src/app/globals.css</files>
  <action>
**1. Create KeyboardShortcutsPanel** (`keyboard-shortcuts-panel.tsx`):
- Create a new React component that renders a popover/dropdown panel listing all keyboard shortcuts
- Props: `isOpen: boolean`, `onClose: () => void`
- Render a positioned panel (absolute, anchored to top-right area below topbar) with class `shortcuts-panel`
- Content: a grid/table of shortcuts grouped by category:
  - **লেখা** (Writing): `Ctrl+Shift+B` — বাংলা/English টগল, `Tab` — সাজেশন গ্রহণ, `Escape` — সাজেশন বাতিল
  - **ফরম্যাটিং** (Formatting): `Ctrl+B` — বোল্ড, `Ctrl+I` — ইটালিক, `Ctrl+U` — আন্ডারলাইন, `Ctrl+D` — স্ট্রাইকথ্রু, `Ctrl+E` — কোড, `Ctrl+H` — হাইলাইট
- Each shortcut row: key combo on left (styled as `<kbd>` elements), description on right
- Click outside or press Escape closes the panel
- Use `useEffect` with document click listener when open, cleanup on close

**2. Wire into NoteComponent** (`index.tsx`):
- Import `KeyboardShortcutsPanel`
- Add `showShortcuts` boolean state (default false)
- Add a button in `.topbar-controls` (before the language toggle): an icon button with a keyboard icon (SVG) or "?" icon, class `btn-shortcuts`, title "কীবোর্ড শর্টকাট" (Keyboard Shortcuts), onClick toggles `showShortcuts`
- Render `<KeyboardShortcutsPanel isOpen={showShortcuts} onClose={() => setShowShortcuts(false)} />` inside the topbar area

**3. Add CSS styles** (`globals.css`):
- `.btn-shortcuts`: 32px square button, transparent background, rounded 8px, color var(--text-tertiary), hover background var(--bg-tertiary) + color var(--text-primary), transition fast, cursor pointer, border none, flex center
- `.shortcuts-panel`: position fixed, top calc(var(--topbar-height) + 8px), right 24px, background var(--bg-elevated), border 1px solid var(--border-primary), border-radius 12px, box-shadow var(--shadow-lg), padding 16px 20px, min-width 320px, max-width 400px, z-index 200, animation popUp 0.2s
- `.shortcuts-panel-title`: font-family var(--font-display), font-size 15px, font-weight 700, color var(--text-primary), margin-bottom 12px, padding-bottom 8px, border-bottom 1px solid var(--border-primary)
- `.shortcuts-group`: margin-bottom 12px
- `.shortcuts-group-title`: font-size 11px, font-weight 600, text-transform uppercase, letter-spacing 0.1em, color var(--text-muted), margin-bottom 6px
- `.shortcut-row`: display flex, justify-content space-between, align-items center, padding 4px 0, font-size 13px, color var(--text-secondary)
- `.shortcut-row kbd`: same style as `.ghost-text-tab-hint kbd` (background var(--bg-tertiary), border, rounded 4px, mono font, font-size 11px, font-weight 600, color var(--text-secondary), padding 2px 6px, box-shadow)
- `.shortcut-row span:last-child`: color var(--text-secondary)
  </action>
  <verify>
    <automated>cd /Users/tareq/codes/Bangla/notebook && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Keyboard shortcut button visible in topbar. Clicking opens a panel listing all shortcuts grouped by category. Panel closes on click outside or Escape. All shortcuts displayed in Bangla with kbd-styled key combos.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. `pnpm dev` starts without errors
3. Creating a new note shows title input above editor
4. Typing a title and content, then switching notes, preserves both
5. Sidebar shows only note titles (no content preview)
6. Old notes without title field load with empty title (no crash)
7. Keyboard shortcut button in topbar opens panel with all shortcuts
8. Panel closes on Escape or clicking outside
</verification>

<success_criteria>
- Note model includes `title` field, persisted in localStorage
- Sidebar displays title-only list (no content snippets)
- Title input renders above editor with Bangla phonetic input support
- Keyboard shortcuts panel accessible from topbar with grouped shortcut listing
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260328-281-polish-ui-ux-note-titles-sidebar-cleanup/260328-281-SUMMARY.md`
</output>
